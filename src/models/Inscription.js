class Inscription {
    constructor(numIns, dateIns, matricule, statut, numClass, anneeScolaire) {
        this.numIns = numIns;
        this.dateIns = dateIns;
        this.matricule = matricule;
        this.numClass = numClass;
        this.statut = statut;
        this.anneeScolaire = anneeScolaire;
    }
}

export default Inscription;
