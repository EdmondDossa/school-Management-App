class Cours {
  constructor(
    numCours,
    heureDebut,
    nombreHeures,
    jour,
    codMat,
    numClass,
    annee
  ) {
    this.Num_Cours = numCours;
    this.HDebut = heureDebut;
    this.NBHeures = nombreHeures;
    this.Jour = jour;
    this.Cod_Mat = codMat;
    this.Num_Class = numClass;
    this.Annee = annee;
  }
}

export default Cours;
